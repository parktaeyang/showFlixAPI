<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.showflix.api.schedule.mapper.AdminNoteMapper">

    <resultMap id="AdminNoteResult" type="com.showflix.api.schedule.domain.AdminNote">
        <result property="id" column="id"/>
        <result property="content" column="content"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="AdminNoteResult">
        SELECT id, content, updated_by, updated_at
        FROM admin_note
        WHERE id = #{id}
    </select>

    <insert id="upsert">
        INSERT INTO admin_note (id, content, updated_by, updated_at)
        VALUES (#{id}, #{content}, #{updatedBy}, NOW())
        ON DUPLICATE KEY UPDATE
            content = VALUES(content),
            updated_by = VALUES(updated_by),
            updated_at = NOW()
    </insert>

</mapper>
